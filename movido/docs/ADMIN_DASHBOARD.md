# 🎛️ لوحة التحكم الإدارية - MOVIDO Admin Dashboard

## 📋 نظرة عامة

تم إنشاء لوحة تحكم إدارية كاملة لموقع MOVIDO تتيح لك التحكم الكامل في المستخدمين والإحصائيات.

## 🔐 الوصول للوحة التحكم

### الرابط المحلي:

```
http://localhost:5173/admin
```

### كلمة المرور الافتراضية:

```
admin123
```

> ⚠️ **مهم**: يمكنك تغيير كلمة المرور من خلال متغير البيئة `ADMIN_PASSWORD` في ملف `.env`

## ✨ المميزات الرئيسية

### 1. 📊 الإحصائيات الشاملة

- **إجمالي المستخدمين**: عدد المستخدمين المسجلين
- **إجمالي الفيديوهات**: عدد الأفلام والمسلسلات
- **ساعات المشاهدة**: إجمالي وقت المشاهدة
- **المستخدمين المميزين**: عدد المستخدمين الحاصلين على شارة Fan

### 2. 👥 إدارة المستخدمين

- **عرض آخر 20 مستخدم** منضم للمنصة
- **تعديل النقاط**: إمكانية تعديل نقاط أي مستخدم
- **حذف المستخدمين**: إزالة المستخدمين غير المرغوب فيهم
- **عرض تفاصيل كل مستخدم**: المعرف، النقاط، وقت المشاهدة، تاريخ الانضمام

### 3. 🏆 قائمة الأكثر مشاهدة

- عرض أفضل 5 مستخدمين من حيث وقت المشاهدة
- ترتيب تلقائي حسب ساعات المشاهدة
- عرض النقاط المكتسبة لكل مستخدم

### 4. ⚙️ إعدادات سريعة

- مسح ذاكرة التخزين المؤقت
- تحديث روابط السيرفرات
- إرسال إشعارات للمستخدمين

## 🎨 التصميم

### الواجهة

- **تصميم عصري**: استخدام Glass Morphism
- **ألوان متناسقة**: نظام ألوان احترافي مع Ice Mint و Red Accents
- **متجاوب**: يعمل على جميع الأجهزة (Desktop, Tablet, Mobile)
- **رسوم متحركة**: Framer Motion للتفاعلات السلسة

### الأمان

- **نظام مصادقة**: كلمة مرور محمية
- **Token System**: استخدام JWT-like tokens
- **Local Storage**: حفظ حالة تسجيل الدخول

## 🔧 البنية التقنية

### Backend (FastAPI)

```
backend/app/api/endpoints/admin.py
```

**Endpoints المتاحة:**

- `POST /api/admin/login` - تسجيل الدخول
- `GET /api/admin/stats` - جلب الإحصائيات
- `POST /api/admin/users/update_points` - تعديل نقاط المستخدم
- `DELETE /api/admin/users/{user_id}` - حذف مستخدم

### Frontend (React + TypeScript)

```
meih-netflix-clone/src/pages/AdminDashboard.tsx
```

**المكونات:**

- Login Form
- Stats Cards
- Users Table
- Top Watchers List
- Edit Modal
- Quick Settings Panel

## 📱 كيفية الاستخدام

### 1. تسجيل الدخول

1. افتح `http://localhost:5173/admin`
2. أدخل كلمة المرور: `admin123`
3. اضغط "دخول المشرف"

### 2. عرض الإحصائيات

- الإحصائيات تظهر تلقائياً بعد تسجيل الدخول
- اضغط زر "تحديث" لتحديث البيانات

### 3. تعديل نقاط مستخدم

1. مرر الماوس على صف المستخدم في الجدول
2. اضغط على أيقونة القلم الأزرق (✏️)
3. أدخل النقاط الجديدة
4. اضغط "حفظ التعديلات"

### 4. حذف مستخدم

1. مرر الماوس على صف المستخدم
2. اضغط على أيقونة سلة المهملات الحمراء (🗑️)
3. أكد الحذف

### 5. تسجيل الخروج

- اضغط زر "خروج" في الأعلى

## 🔒 الأمان والحماية

### تغيير كلمة المرور

أضف في ملف `.env` في مجلد `backend`:

```env
ADMIN_PASSWORD=your_secure_password_here
```

### الحماية من الوصول غير المصرح

- كل API endpoint محمي بـ Authorization header
- التحقق من الـ token في كل طلب
- إعادة توجيه تلقائية عند انتهاء الجلسة

## 🎯 الميزات المستقبلية (اختياري)

يمكنك إضافة:

- 📧 إرسال إشعارات للمستخدمين
- 📈 رسوم بيانية للإحصائيات
- 🔍 بحث وفلترة المستخدمين
- 📊 تصدير البيانات (CSV/Excel)
- 🎬 إدارة المحتوى (إضافة/حذف أفلام)
- 📝 سجل النشاطات (Activity Log)
- 👤 إدارة صلاحيات متعددة

## 🚀 الوصول السريع

### من Footer الموقع

تم إضافة رابط "لوحة التحكم" في قسم الدعم في الفوتر

### الرابط المباشر

```
http://localhost:5173/admin
```

## 📞 الدعم

إذا واجهت أي مشاكل:

1. تأكد من تشغيل Backend على المنفذ 8000
2. تأكد من تشغيل Frontend على المنفذ 5173
3. تحقق من Console للأخطاء
4. تأكد من صحة كلمة المرور

---

**تم التطوير بواسطة**: Antigravity AI  
**التاريخ**: 2026-01-13  
**الإصدار**: 1.0.0
